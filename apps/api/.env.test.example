# Copy to .env.test and provision this disposable database before running tests.
# .env supplies other development defaults; .env.test must override the DB target.
# Tests rebuild tables. Never use a development, shared or production database.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/carta_api_test
CARTA_DATABASE_PURPOSE=test
CARTA_TEST_DATABASE_NAME=carta_api_test
