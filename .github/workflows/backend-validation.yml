name: Backend Validation

on:
  pull_request:
    paths:
      - 'packages/sprindle/**'
      - 'apps/api/**'
      - 'pnpm-workspace.yaml'
      - 'pnpm-lock.yaml'
      - 'tsconfig.base.json'
      - '.github/workflows/backend-validation.yml'
  push:
    paths:
      - 'packages/sprindle/**'
      - 'apps/api/**'
      - 'pnpm-workspace.yaml'
      - 'pnpm-lock.yaml'
      - 'tsconfig.base.json'
      - '.github/workflows/backend-validation.yml'

permissions:
  contents: read

concurrency:
  group: backend-validation-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: carta_api_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/postgres
      API_PORT: '3000'
      BETTER_AUTH_URL: http://localhost:3000
      # Dummy CI value, not a real credential.
      BETTER_AUTH_SECRET: ci-secret-value-that-is-long-enough-32

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 12.1.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint sprindle
        run: pnpm --filter @southneuhof/sprindle lint

      - name: Type-check sprindle
        run: pnpm --filter @southneuhof/sprindle type-check

      - name: Test sprindle
        run: pnpm --filter @southneuhof/sprindle test

      - name: Lint api
        run: pnpm --filter @southneuhof/api lint

      - name: Type-check api
        run: pnpm --filter @southneuhof/api type-check

      - name: Configure isolated api test database
        run: |
          cat > apps/api/.env.test <<'EOF'
          DATABASE_URL=postgres://postgres:postgres@localhost:5432/carta_api_test
          CARTA_DATABASE_PURPOSE=test
          CARTA_TEST_DATABASE_NAME=carta_api_test
          EOF

      - name: Test api
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/carta_api_test
        run: pnpm --filter @southneuhof/api test
